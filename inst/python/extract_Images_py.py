import os
import cv2 as cv


def extract_Images_py(vids, weights, n, path):
  
  # Get group names
  names = list(weights.keys())
  
  # Calculate the number of images to export in each group
  
  
  
